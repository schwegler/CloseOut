<%= semantic_form_for @tech do |f| %>
	<%= f.inputs do %>
	    <%= f.input :name %><br />
	    <%= f.input :phone %><br />
	<% end %>
	<%= f.actions do %>
	    <%= f.action :submit, :as => :input, :label => "Submit" %>
	    <%= f.action :cancel, :label => "Cancel" %>
    <%end %>

<% end %>
